pandas>=2.0,<3.0    # <3.0 required by ydata-profiling, not by our own code
PyYAML>=6.0
Faker>=20.0
ydata-profiling>=4.0   # industry-standard EDA report; complements, doesn't
                        # replace, the contract-driven profiler in src/profiling/
setuptools<81          # ydata-profiling imports pkg_resources without declaring
                        # it as a dependency; setuptools>=81 removed it entirely
